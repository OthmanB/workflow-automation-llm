# Public Project Configuration

[`example.yaml`](example.yaml) is the sanitized public schema-v1 reference.
It documents all required configuration sections and is validated by the test
suite.

Do not add private project paths, credentials, historical evidence, or real
repository details to public configuration. Private-project migration is a
separately authorized operation and is not required to configure a new public
project.
